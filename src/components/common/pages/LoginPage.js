import "./pages.css"
import LoginButtons from "../buttons/login/LoginButtons";

function LoginPage() {
    return (
        <div className="page login">
            <LoginButtons wide />
        </div>
    );
}

export default LoginPage;
